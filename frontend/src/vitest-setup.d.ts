// Activates @testing-library/jest-dom's matcher type augmentation for Vitest's `expect`
// (toBeInTheDocument, toHaveAttribute, …) across the typechecked program.
import '@testing-library/jest-dom/vitest';
